<?xml version="1.0" encoding="utf-8"?>
    // Do 2 - 7 in main.xml ///////////////////////////////////////////////////////////////////////
    // OK (2) Create a menu xml called 'main.xml' in the res->menu folder
    // OK (3) Add one menu item to your menu
    // OK (4) Give the menu item an id of @+id/action_search
    // OK (5) Set the orderInCategory to 1
    // OK (6) Show this item if there is room (use app:showAsAction, not android:showAsAction)
    // OK (7) Set the title to the search string ("Search") from strings.xml
    // Do 2 - 7 in main.xml ///////////////////////////////////////////////////////////////////////
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_search"
        android:orderInCategory="1"
        app:showAsAction="ifRoom"
        android:title="@string/search"/>
</menu>